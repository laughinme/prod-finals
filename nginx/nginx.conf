server {
    listen 80;
    listen [::]:80;
    server_name _;

    client_max_body_size 50m;
    resolver 127.0.0.11 valid=30s ipv6=off;
    resolver_timeout 5s;

    location /api/ {
        set $backend_upstream backend:8080;
        proxy_pass http://$backend_upstream$request_uri;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
        proxy_read_timeout 120s;
    }

    # S3-compatible media access via same origin:
    # /<bucket>/<object_key>?X-Amz-...
    # Keep original Host header for valid presigned URL signature.
    location ~ ^/(media-public|media-private)/ {
        set $minio_upstream minio:9000;
        proxy_pass http://$minio_upstream$request_uri;
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_request_buffering off;
        proxy_buffering off;
        proxy_read_timeout 300s;
    }

    # Serve SPA static build from nginx
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
